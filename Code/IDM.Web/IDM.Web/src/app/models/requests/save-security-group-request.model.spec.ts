import { SaveSecurityGroupRequest } from './save-security-group-request.model';

describe('SaveSecurityGroupRequest', () => {
  it('should create an instance', () => {
    expect(new SaveSecurityGroupRequest()).toBeTruthy();
  });
});
