import { SecurityGroupDTO } from './security-group-dto.model';

describe('SecurityGroupDTO', () => {
  it('should create an instance', () => {
    expect(new SecurityGroupDTO()).toBeTruthy();
  });
});
